source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in activeadmin-dry-validation.gemspec
gemspec

gem 'rails', '~> 5'
gem 'rails-controller-testing'

group :development do
  gem 'listen'
  gem 'sqlite3'
end
