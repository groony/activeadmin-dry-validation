RSpec.describe ActiveAdmin::Dry::Validation do
  it "has a version number" do
    expect(ActiveAdmin::Dry::Validation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
